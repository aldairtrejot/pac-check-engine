
/**
 * The function creates the messages so that they can be shown to the user, 
 * the configuration depends on milliseconds and the location of this
 */

var notyfEM = new Notyf({
    position: {
        x: 'right',
        y: 'top',
    },
    dismissible: true,
    duration: 6000,
});